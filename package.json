{
  "name": "notion-recipe-stats",
  "version": "1.0.0",
  "type": "module",
  "main": "src/index.js",
  "scripts": {
    "build": "tsc --project .",
    "prestart": "tsc --project .",
    "start": "node src/index.js",
    "format": "prettier **/*.ts --write",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {
    "@js-temporal/polyfill": "^0.4.1",
    "@notionhq/client": "^1.0.4",
    "tslib": "^2.4.0"
  },
  "devDependencies": {
    "@types/node": "^18.0.0",
    "dotenv": "^16.0.1",
    "prettier": "^2.7.1",
    "typescript": "^4.7.4"
  },
  "engines": {
    "node": ">=18"
  }
}
